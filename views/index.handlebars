
<div class="bg-light p-5 rounded-lg m-3">
  <h1 class="display-4">Bem vindo(a) ao Blog NodeJS!!</h1>
  <p class="lead">Este é um BLOG simples, desenvolvido em Node.Js + express + MongoDB</p>
  <hr class="my-4">
  <p>Aqui você pode ler sobre tópicos diversos!</p>
  <a class="btn btn-primary btn-lg" href="#" role="button">Crie uma conta</a>
</div>
<hr>
<h2>Postagens recentes:</h2>

{{#each postagens}}

<div class="card mt-4">
    <div class="card-body mt-4">
        <h3>{{titulo}}</h3>
        <p>{{descricao}}</p>
        <a href="/postagem/{{slug}}"><button class="btn btn-success">Leia Mais</button></a>
        <hr>
        <small>{{categoria.nome}}</small><br>
        <small>Data de publicação: {{data}}</small>
    </div>
</div>

{{else}}
<h5>Nenhuma postagem encontrada...</h5>
{{/each}}

