{{#each erros}}
    <div class="alert alert-danger">{{text}}</div>
{{else}}

{{/each}}

<h3>Nova Categoria</h3>

<div class="card">

    <div class="card-body">
        <form action="/admin/categorias/nova" method="post">
            <label for="nome">Nome: </label>
            <input type="text" id="nome" name="nome" placeholder="Nome da Categoria" class="form-control">
            <br>
            <label for="slug">Slug: </label>
            <input type="text" id="nome" name="slug" placeholder="Slug da Categoria" class="form-control">
            <br>
            <button class="btn btn-success">Criar Categoria</button>
        </form>
    </div>

</div>